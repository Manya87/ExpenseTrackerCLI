# ExpenseTrackerCLI 📈

A robust Java-based financial management tool designed for precision and data persistence.

### 🚀 Technical Features
* **Custom Serialization:** Implemented `java.io.Serializable` for local data persistence without a database.
* **Financial Precision:** Utilized `BigDecimal` for currency to eliminate floating-point arithmetic errors.
* **Functional Programming:** Used the **Java Streams API** for data aggregation and display.
* **Maven Architecture:** Standardized build lifecycle and dependency management.

### 🛠️ How to Run
1. Clone the repo.
2. Ensure you have JDK 11+ installed.
3. Run `java -cp target/classes com.tracker.Main`
